services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - pkglens-data:/app/.data
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
    restart: unless-stopped

volumes:
  pkglens-data:
